@model IEnumerable<JVStoreFront.DATA.EF.MediaType>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MediaType1)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MediaType1)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MediaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MediaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MediaID })
        </td>
    </tr>
}

</table>
